version: '3.9'

services:

  apache:
    container_name: ${APACHE_NAME}
    build:
      context: .
      args:
        TAG: ${APACHE_TAG}
        ENV: ${ENV}
    restart: always
    networks:
      - default
    ports:
      - ${APACHE_HTTP_PORT}:8080
      - ${APACHE_HTTPS_PORT}:8443
    environment:
      ENV: ${ENV}
